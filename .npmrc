# Apps import each other via bare package names (e.g. `refi-portal/src/views/customer`)
# and rely on transitive peers being reachable. Hoisting keeps that resolution working
# the same way the upstream `file:../x` npm setup did.
shamefully-hoist=true
strict-peer-dependencies=false
auto-install-peers=true
